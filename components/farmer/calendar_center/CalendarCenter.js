import React from 'react';
import { View, Text } from 'react-native';

const CalendarCenter = ({ navigation }) => {
  return (
    <View>
      <Text>CalendarCenter</Text>
    </View>
  );
};

export default CalendarCenter;
